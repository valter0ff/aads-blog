.bg-light.py-5.my-2
  %h2.text-center.text-primary= t('.feed')
  .row.justify-content-md-center
    .col-8.text-center
      - if @posts.any?
        %ul.list-group
          = render @posts
      - else
        .alert.alert-warning.text-center
          = t('.no_posts_found')
    - if @pagy.pages > 1
      .col-10.justify-content-center.pagination.mt-4
        != pagy_bootstrap_nav @pagy
